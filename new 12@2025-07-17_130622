pipeline {
    agent any

    environment {
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-cred-id') // Jenkins credentials ID
        IMAGE_NAME = "yourdockerhubusername/nodejs-app"
    }

    stages {
        stage('Checkout Code') {
            steps {
                git 'https://github.com/your-org/your-nodejs-app.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

        stage('Run Tests') {
            steps {
                sh 'npm test'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("${IMAGE_NAME}:${env.BUILD_NUMBER}")
                }
            }
        }

        stage('Push to DockerHub') {
            steps {
                script {
                    docker.withRegistry('', DOCKERHUB_CREDENTIALS) {
                        docker.image("${IMAGE_NAME}:${env.BUILD_NUMBER}").push()
                    }
                }
            }
        }
    }

    post {
        success {
            echo "Build and push successful!"
        }
        failure {
            echo "Build failed."
        }
    }
}

=======================
SECURE JENKINS:


| Area             | Security Measure                               |
| ---------------- | ---------------------------------------------- |
| RBAC             | Role-based access via plugin                   |
| Secrets          | Stored in credentials store, not in code       |
| Pipeline usage   | `withCredentials` block with scoped access     |
| Network          | Use HTTPS, restrict agent/master communication |
| Authentication   | SSO, GitHub OAuth, LDAP                        |
| Logging/Auditing | Audit Trail, Job Configuration History         |
